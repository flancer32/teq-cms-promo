/**
 * Plugin constants (hardcoded configuration) for backend code.
 */
export default class Fl32_Cms_Demo_Back_Defaults {
    DIR_WEB = 'web';
    LOCALE_ALLOWED = ['en', 'ru'];
    LOCALE_DEFAULT = 'en';
    PORT = 3000;
}
